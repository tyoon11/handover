# Vital 요약 생성규칙

수술 중 vital sign 시계열을 **규칙 기반(rule-based)** 으로 한 줄씩 요약 문자열로 변환하는 규칙 정의서입니다.
구현: [utils/vital_summarizer.py](utils/vital_summarizer.py) ·
임계값: [utils/vital_thresholds.py](utils/vital_thresholds.py) · 컬럼 상수: [config.py](config.py)

> 목적: LLM 인계문 생성 시 프롬프트에 삽입되는 `### INTRAOPERATIVE VITAL SUMMARY` 블록과,
> faithfulness 평가의 근거(premise)로 쓰이는 vital 요약 텍스트를 결정적(deterministic)으로 만든다.

> **임계값 개정 (v3.1)** — 모든 임계값을 Smith's Anesthesia (2021) / Miller's Anesthesia (2024)
> 근거로 재설정했다. 표·페이지 단위 전거와 v1 대비 변경 내역은
> **[docs/THRESHOLDS.md](docs/THRESHOLDS.md)** 에 있다. 아래 §4는 그 결과의 요약이다.

---

## 1. 입력 데이터 스키마

| 역할 | 컬럼명 | 비고 |
|------|--------|------|
| 수술 ID | `수술ID` (`VIT_SID_COL`) | int64, **공백 없음** — EMR의 `"수술 ID"`(공백 있음)와 다름 |
| 기록 시각 | `기록일시` (`VIT_TIME_COL`) | `to_datetime`으로 파싱 |
| 항목명 | `진료서식구성원소명` (`VIT_ITEM_COL`) | 예: `"HR <- HR <- 마취기록"` |
| 값 | `진료서식구성원소내용` (`VIT_VAL_COL`) | object → numeric 변환 |

### 대상 항목 (`VITAL_ITEMS`)
`HR, MBP, DBP, SBP, IMBP1, ISBP1, IDBP1, SpO2, Ppeak, T1, QTc, EBL, UO`

- **비침습 혈압 (`BP_ITEMS`)**: `MBP, DBP, SBP`
- **침습 혈압 (`IBP_ITEMS`)**: `IMBP1, ISBP1, IDBP1`

---

## 2. 공통 전처리 규칙

1. **항목명 파싱**: `"HR <- HR <- 마취기록"` → `" <- "` 기준 split 후 **첫 토큰**만 사용 → `"HR"`.
2. **숫자 변환**: 값을 numeric으로 강제 변환(`errors="coerce"`), 비숫자는 `NaN`.
3. **0 제거**: `0` 값은 **센서 미연결/오입력**으로 간주하여 `NaN` 처리 후 제거.
   - 단, `EBL`/`UO`는 이 0-제거 규칙을 적용하지 않고 별도 처리(누적 최대값).
4. **정렬**: 항목별 시계열은 `기록일시` 오름차순 정렬.
5. **데이터 없음**: 해당 항목 시계열이 비어 있으면 그 줄은 **출력하지 않는다**(빈 문자열 제거).
   - 수술 전체에 vital이 없으면 `"(바이탈 데이터 없음)"` 반환.

### 통계 표기 포맷 (`_stat`)
```
중앙값 {median} {unit} (범위 {min}–{max} {unit})
```
- 값은 소수점 없이 반올림(`.0f`). **체온만 소수 1자리** — 35.3과 35.8은 판정 tier가 다르다.
- 값이 없으면 `"데이터 없음"`.

### 이벤트 표기 규칙 — **2-tier + 지속시간·편차**

이상 소견은 "몇 회"만으로는 인계가 안 된다. **얼마나 오래**, **기준 대비 얼마나** 벗어났는지를
항상 함께 적는다.

```
[유의]저혈압(<70): 4회 20분(최장 15분), 최저 55 (기준-15)
```

| 조각 | 뜻 |
|---|---|
| `[유의]` | 임상적으로 유의 (소생·개입 기준 초과) |
| `저혈압(<70)` | 이벤트명과 적용된 임계값 |
| `4회` | 위반 표본 수 |
| `20분` | 총 지속시간 (모든 episode 합) |
| `(최장 15분)` | 최장 연속 episode — episode가 2개 이상일 때만 |
| `최저 55 (기준-15)` | 최저/최고값과 기준 대비 편차 |

| 표기 | 뜻 |
|---|---|
| `[유의]{이벤트}` | 소생·개입 기준 초과 = **임상적으로 유의**. 인계문에 반드시 들어가야 한다. |
| `{이벤트}` (`[유의]` 없음) | 연령별 정상 참조범위 이탈 = 통계적 이상. |

- 이벤트가 있으면 → `, 이벤트: {[유의] 이벤트} | {범위 이탈}` — `[유의]`가 항상 앞, 구분자는 ` | `.
- 하나도 없으면 → `, 이상 없음`.
- 두 tier는 **배타적으로** 센다. 유의 기준을 넘은 값은 "범위 이탈"에 이중계수하지 않는다.

#### 지속시간 산정

- **episode** = 임계값을 연속으로 위반한 구간. `(마지막 − 첫 기록 시각) + 표본간격 1구간`.
  표본 1건뿐인 episode는 `표본간격`(보통 5분)으로 계산한다.
- **표본간격** = 그 항목 시계열의 기록 간격 **중앙값**. 추정 불가 시 5분(`DEFAULT_STEP_MIN`).
- 따라서 지속시간은 이산 표본을 구간으로 환산한 **추정치**다.
  [M] Ch.4가 술중저혈압을 *"time or fraction of the case below threshold"* 로 정량화하라고
  권하는 방식과 같다 (PDF p.226).
- 1분 미만은 `<1분`, 60분 이상은 시간 단위(`1.5시간`)로 표기.

> 왜 나눴나: v1의 단일 임계값은 QTc 연장·일시적 SpO2 저하처럼 임상적으로 무의미한
> 이벤트를 대량 생성해 gold checklist를 오염시켰다
> ([PROMPTS_V3.md](PROMPTS_V3.md) L235·L249).

---

## 3. 소아 연령 그룹 (`age_group`)

`수술당시나이(age_years)` 기준. 표기·리포트용이며, **임계값 자체는 연속 나이로 계산**한다
(HR은 [S] Table 18.1의 9개 원구간을 그대로 쓴다).

| 그룹 | 나이 범위 |
|------|-----------|
| `neonate` (신생아) | age < 28일 |
| `infant` (영아) | 28일 ≤ age < 1 |
| `toddler` (유아) | 1 ≤ age < 3 |
| `preschool` (학령전) | 3 ≤ age < 6 |
| `school` (학령기) | 6 ≤ age < 12 |
| `adolescent` (청소년) | age ≥ 12 |

- **`neonate`는 v3.1에 신설** — PALS 저혈압 기준이 신생아만 SBP < 60으로 다르고
  ([S] Table 57.3), QTc 정상 상한도 신생아만 470 ms다 ([S] Ch.5).
- 나이 매핑 실패 시 기본값 **5세(preschool)** 사용.
- 출력 첫 줄에 `(연령군: 영아(28일–1세))` 형태로 적용 연령군을 명시한다 — 임계값이
  나이에 따라 달라지므로 판정 근거를 사람이 바로 확인할 수 있어야 한다.

---

## 4. 항목별 요약 규칙

전거·페이지는 전부 [docs/THRESHOLDS.md](docs/THRESHOLDS.md).
**[S]** = Smith's Anesthesia 2021, **[M]** = Miller's Anesthesia 2024.

### 4.1 HR (심박수, bpm)

| tier | 기준 | 전거 |
|---|---|---|
| `[유의]서맥` / `[유의]빈맥` | 서맥 < 60 (전 연령) · 빈맥 > 220(신생아) / 190(영아) / 180(1–10세) / 150(>10세) | [S] Table 57.3 (PALS) |
| `정상하한 미만` / `정상상한 초과` | 연령별 mean ± 2 SD 범위 밖 | [S] Table 18.1 |

정상 참조범위(9개 원구간): 0–24h `94–145` · 1–7d `100–175` · 8–30d `115–190` ·
1–3m `124–190` · 3–12m `111–179` · 1–3y `98–163` · 3–5y `65–132` · 5–8y `70–115` · 8–16y `55–105`

- 출력 예: `HR: 중앙값 140 bpm (범위 55–150 bpm), 이벤트: [유의]서맥(<60) 1회`
- v1(infant 100/160 등)은 이 표와 양방향으로 어긋나 정상 영아를 빈맥으로,
  정상 학령전아를 서맥으로 오표기하고 있었다.

### 4.2 혈압 (mmHg)
비침습(`SBP/MBP/DBP`)과 침습(`ISBP1/IMBP1/IDBP1`)에 **동일 규칙** 적용.

**SBP / ISBP1 (수축기)**
- `[유의]저혈압` — [S] Table 57.3 (PALS): 신생아 < 60 / 영아 < 70 / 1–10세 < `70 + 2×나이` / >10세 < 90
- `고혈압` — [S] Table 18.2 연령별 95th %ile 초과.
  1세 104 · 2세 106 · 3세 109 · 4세 111 · 5세 112 · 6세 114 · 7세 115 · 8세 116 ·
  9세 118 · 10세 119 · 11세 121 · 12세 123 · <1세 100(관례) · ≥13세 130(AAP 2017)

**MBP / IMBP1 (평균)**
- `[유의]저혈압` — `min(1.5 × 나이 + 40, 65)`.
  관례식에 [M] Ch.4 성인 술중저혈압 기준(MAP < 65)을 상한으로 씌운 것. 16.7세에서 수렴.

**DBP / IDBP1 (이완기)**
- `고(>{hi})` — [S] Table 18.3 95th %ile 초과.
  1세 58 · 2세 63 · 3세 67 · 4세 70 · 5세 72 · 6세 74 · 7세 76 · 8세 78 ·
  9세 79 · 10세 80 · 11세 80 · 12세 81 · <1세 65(관례) · ≥13세 80
- **하한은 판정하지 않는다.** 두 교과서에 소아 이완기 저혈압 수치 기준이 없다
  (PALS·TAXI 모두 관류 지표로 SBP/MAP만 사용). 이벤트가 없으면 `, 상한 이상 없음`으로 표기해
  "하한을 안 본다"는 사실이 출력에 드러나게 했다. v1의 `infant 30 / adolescent 45`는 출처 불명이라 제거.

> **침습 혈압(IBP)은 데이터가 존재할 때만 출력**한다(`IMBP1/ISBP1/IDBP1` 중 하나라도 값이 있으면 전부 출력, 없으면 전부 생략).

### 4.3 SpO2 (산소포화도, %)
[S] Ch.57 목표 94–99%.
- `[유의]SpO2<90%` — 탈포화
- `목표미달(90–93%)` — 목표 이탈
- 두 tier를 **각각** 센다(v1은 배타적 우선순위라 `<90`이 있으면 `<95` 건수가 사라졌다).

### 4.4 Ppeak (기도 최고압, cmH₂O)
- **임계값 없음** — 두 교과서에 소아 일반마취용 기준이 없다([S]의 25/28 cmH₂O는 선천성
  횡격막 탈장 신생아 전용 맥락). 통계만 출력하고 판정하지 않는다.
- 출력 예: `Ppeak: 중앙값 18 cmH₂O (범위 12–24 cmH₂O)`

### 4.5 체온 T1 (°C)
[S] Ch.21 안전범위 35.5–37.5 / Ch.7 술중 저체온 정의 < 36.0.

| 표기 | 기준 |
|---|---|
| `[유의]저체온(<35.5)` | 안전범위 하한 미만 |
| `저체온(<36.0)` | 술중 저체온 정의 |
| `안전범위 초과(>37.5)` | 안전범위 상한 초과 |
| `[유의]발열(>38.0)` | 발열 (MH 감별 구간) |

- 통계는 **소수 1자리**로 출력한다.

### 4.6 QTc (ms)
- `[유의]연장` — > 480 ([M]; 성별 미상이므로 보수적으로 480)
- `정상상한 초과` — > 470(신생아) / > 440(그 외) ([S] Ch.5)
- 정상: `QTc: 정상 범위 (최대 {max}ms)`
- v1(450/460 단일 기준)은 임상적으로 무의미한 "연장"을 대량 생성했다.

### 4.7 EBL (추정 실혈량, mL)
- 시계열 **최대값**(누적값) 출력. 값 없으면 생략.
- **체중을 알면 EBV 대비 %를 함께 출력**한다 — 소아에서 절대 mL은 의미가 없다
  (5 kg 영아의 100 mL ≈ EBV의 24%).
  EBV([S] Table 21.6): <3개월 85 · 3개월–1세 78 · 1–6세 73 · >6세 68 mL/kg
- `[유의]유의 실혈` > 10% EBV · `[유의]대량 실혈` > 50% EBV ([S] Ch.18 동맥압 감시 적응증 기준)
- 출력 예: `EBL: 60 mL (EBV의 11%) [유의]유의 실혈(>10% EBV)`

### 4.8 UO (소변량, mL)
- 값 중 `NF` / `NO FOLEY` 패턴이 있으면 → `UO: No Foley`
- 아니면 누적 최대값 출력: `UO: {total} mL`
- **체중(weight_kg)** 이 주어지면 시간당 속도 추가:
  - `rate = total / (weight_kg × 실제 경과시간[hr])` → `({rate} mL/kg/hr)`
  - 경과시간은 UO 행의 타임스탬프 `max − min`. UO 행이 1건뿐이면 케이스 전체 기록 구간 사용.
  - `[유의]핍뇨(<0.5 mL/kg/hr)` — [M] Ch.24 핍뇨 정의
  - 출력 예: `UO: 120 mL (1.5 mL/kg/hr)`

> **v1 버그 수정**: v1은 `rate = total / (weight × 기록건수 / 60)`, 즉 *기록 1건 = 1분*을
> 가정했다(아래 §7 B8). 실제 기록 간격이 5분 안팎이라 속도가 약 5배 과대평가되어
> 핍뇨가 전혀 검출되지 않았다.

---

## 5. 출력 순서 및 조립

`summarize_vitals()`는 아래 순서로 각 줄을 만들고, **빈 줄을 제외**한 뒤 `\n`으로 연결한다.

0. `(연령군: …)` — 적용된 연령군 (임계값 판정 근거)
1. HR
2. 비침습 BP — `MBP → DBP → SBP` (`BP_ITEMS` 순서)
3. 침습 BP — 데이터 있을 때만 `IMBP1 → ISBP1 → IDBP1`
4. SpO2
5. Ppeak — 데이터 있을 때만
6. 체온(T1)
7. QTc
8. EBL
9. UO

### 출력 예시 (6개월 영아, 7 kg, 표본간격 5분)
```
(연령군: 영아(28일–1세))
HR: 중앙값 139 bpm (범위 52–150 bpm), 이벤트: [유의]서맥(<60): 3회 15분, 최저 52 (기준-8)
MBP: 중앙값 45 mmHg (범위 34–55 mmHg), 이벤트: [유의]저혈압(<41): 4회 20분(최장 15분), 최저 34 (기준-7)
DBP: 중앙값 38 mmHg (범위 28–45 mmHg), 상한 이상 없음
SBP: 중앙값 74 mmHg (범위 55–90 mmHg), 이벤트: [유의]저혈압(<70): 4회 20분(최장 15분), 최저 55 (기준-15)
SpO2: 중앙값 99 % (범위 85–100 %), 이벤트: [유의]SpO2<90%: 2회 10분, 최저 85% (기준-5) | 목표미달(90–93%): 1회 5분, 최저 92% (기준-2)
체온(T1): 중앙값 36.2 °C (범위 34.9–36.5 °C), 이벤트: [유의]저체온(<35.5): 2회 10분, 최저 34.9 (기준-0.6) | 저체온(<36.0): 1회 5분, 최저 35.8 (기준-0.2)
QTc: 정상상한 초과(>440ms): 3회 15분, 최고 470ms (기준+30) — 연장기준 480ms 미만
EBL: 60 mL (EBV의 11%) [유의]유의 실혈(>10% EBV)
UO: 5 mL (1.2 mL/kg/hr)
```

- HR 139~150은 6개월 영아 정상 범위(111–179) 안이라 이벤트가 아니다. v1 기준(빈맥 >160)
  으로도 통과했겠지만, HR 165였다면 v1은 빈맥으로 오표기했을 값이다.
- SBP는 `20분(최장 15분)` — 15분 연속 1회 + 단발 1회로 episode가 2개다. 같은 "4회"라도
  15분 연속인지 5분씩 흩어진 건지는 임상적 의미가 전혀 다르므로 최장 episode를 병기한다.

---

## 6. 전체 매핑 (`build_vital_map`)

- 모든 수술 ID에 대해 `{수술ID(int): vital_summary_str}` dict 생성.
- **나이 매핑**: EMR df의 `"수술 ID"`(공백 있음) ↔ `("수술", "수술당시나이", "")` 컬럼.
  - 매핑 실패 시 전 케이스 기본값 5세.
- 500건마다 진행 로그 출력.

---

## 7. 알려진 가정 및 개정 메모

### 해결됨 (v3.1)
- ~~**UO 속도 가정(B8)**~~ — `기록 1건 = 1분` 가정을 실제 타임스탬프 경과 시간으로 교체.
  이제 [M] Ch.24 핍뇨 기준(<0.5 mL/kg/hr)이 실제로 작동한다.
- ~~**QTc 연장·일시적 SpO2 저하의 checklist 오염**~~ — 2-tier 판정(`[유의]` = 임상적 유의)으로
  분리. QTc는 480 ms 초과만 `[유의]`가 된다. checklist 추출 프롬프트의 "vital 파생 항목 추가 금지"
  규칙은 그대로 유지한다 (참고: [PROMPTS_V3.md](PROMPTS_V3.md) L235, L249).
- ~~출처 불명 임계값~~ — 전 항목을 교과서 근거로 재설정. [docs/THRESHOLDS.md](docs/THRESHOLDS.md).

### 남은 가정
- **나이 파싱(B7)** — EMR `("수술","수술당시나이","")` 매핑 실패 시 5세 기본값. 미해결.
- **MAP 소아 절대 기준** — 두 교과서에 표가 없어 관례식 `1.5×나이+40`에 [M]의 성인 기준(65)을
  상한으로 씌운 형태. de Graaff 2016 (*Anesthesiology* 125:904–913) 원논문 수치표를 확보하면 교체.
- **<1세 고혈압 기준** — [S] Table 18.2/18.3이 1세부터라 <1세는 관례값(SBP 100 / DBP 65).
- **미숙아** — [S] Table 21.6 미숙아 EBV(90–100 mL/kg), Table 18.5 미숙아 혈압 참조범위는
  재태주수·일령이 EMR에서 확보되면 적용 가능.
- **baseline 대비 상대 저혈압** — [M] Ch.4의 `baseline 20% 하강` 기준. `map_relative_hypotension()`
  으로 노출해 뒀으나 유도 전 baseline MAP을 시계열에서 분리하지 못해 미사용.

---

## 8. 인계문 필수 항목군

vital 요약과는 별개로, **인계문 자체**가 반드시 다뤄야 할 6개 항목군(기저질환·약물 /
기도관리 / 수술 중 이벤트 및 처치 / 수혈·수액 / 수술 전 검사이상 / 감기 유무)은
[docs/REQUIRED_CATEGORIES.md](docs/REQUIRED_CATEGORIES.md)에 정의돼 있다.
`[유의]` 이벤트는 그중 **3. 수술 중 이벤트 및 처치**의 주요 입력이 된다.
